import type { Config } from "prettier";

const config: Config = {
    useTabs: false,
    semi: true,
    singleQuote: false,
    trailingComma: "all",
    printWidth: 100,
    arrowParens: "always",
    endOfLine: "lf",
    bracketSpacing: true,
    bracketSameLine: false,
    tabWidth: 4,
};

export default config;
